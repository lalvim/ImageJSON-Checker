<!DOCTYPE html>
<html>
  <head>
    <title>Visualizando {{ fileName }}</title>
  </head>
  <body>
    <h1>Arquivo: {{ fileName }}</h1>

    <div>
      <h2>Conteúdo do JSON</h2>
      <pre>{{ JSON.stringify(jsonData, null, 2) }}</pre>
    </div>

    <div>
      <h2>Imagem</h2>
      <img src="/images/{{ imageName }}" alt="Imagem associada" width="300" />
    </div>

    <div>
      <h2>Rotular</h2>
      <form method="POST" action="{{ route('labels.store') }}">
        @csrf
        <input type="hidden" name="fileName" value="{{ fileName }}" />

        <div>
          <label>
            <input type="radio" name="isCorrect" value="true"
              @if(existingLabel && existingLabel.isCorrect) checked @endif
            />
            Verdadeiro
          </label>
          <label>
            <input type="radio" name="isCorrect" value="false"
              @if(existingLabel && !existingLabel.isCorrect) checked @endif
            />
            Falso
          </label>
        </div>

        <div>
          <label for="observation">Observação</label><br/>
          <textarea name="observation" id="observation" rows="3" cols="50">{{ existingLabel ? existingLabel.observation : '' }}</textarea>
        </div>

        <button type="submit">Salvar</button>
      </form>
    </div>

    <p><a href="{{ route('labels.index') }}">Voltar</a></p>
  </body>
</html>
